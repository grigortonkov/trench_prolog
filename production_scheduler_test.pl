% ["production_scheduler.pl"].

schedule_new_order(p1, 1, date(2026, 1, 31), 3).

schedule_new_order(p1, 1, date(2026, 1, 31), 3).

schedule_new_order(p1, 1, date(2026, 1, 15), 3).

schedule_new_order(p1, 1, date(2026, 1, 30), 3).

list_all_orders.

query_order_schedule(1000).

query_order_schedule(1001).

query_order_schedule(1002).

query_order_schedule(1003).

